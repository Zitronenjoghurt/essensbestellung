# page-level style
login_page_style = {
   "background":"linear-gradient(to top right, #13408d, #15479b, #0f316c)",
   "height":"100vh"
}

# container style
login_container_style = {
    "box_shadow": "md",
    "border_radius": "lg",
    "padding": "2em",
    "background": "rgba(0, 0, 0, 0.7)",
    "size": "1"
}

# text styles
text_style = {
    "font_size": "2em",
    "margin_bottom": "0.5em",
    "font_weight": "bold",
}

# input styles
input_style = {
    "margin_bottom": "0.5em",
    "border": "blue solid 0.5px"
}

# button styles
button_style = {
    "margin": "1em 0.5em 0 0"
}